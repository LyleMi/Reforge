# Documentation investigations

Treat each binary's Clap metadata and the serialized schema 26 Issue envelope as authoritative. Parse every runnable documented command. Distinguish current declarations from migration notes. Reject removed report fields and umbrella commands as current behavior. Keep examples, generated help, report schema, user guide, and skills consistent.
