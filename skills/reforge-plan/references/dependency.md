# Dependency investigations

Inspect cycle or hub edges, unresolved edges, fan-in/fan-out, transitive context, public contracts, adapters, and reachable tests. Prefer moving a stable contract, inverting one edge, or narrowing an interface over broad relocation. Mark issues as conflicting when they touch the same boundary or test fixture.
